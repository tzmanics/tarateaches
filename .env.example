# Anthropic API key for the /plaiy feature.
# NEVER commit a real key. Set this in the Cloudflare Pages dashboard
# (Settings -> Environment variables) or via `wrangler secret put`.
ANTHROPIC_API_KEY=sk-ant-your-key-here
